#include "SDL.h"

int main(int argc, char **argv)
{

    int a;
/*     SDL_Window test; */
    SDL_SetWindowDisplayMode(SDL_Window *window, const SDL_DisplayMode *mode);
    SDL_DisplayMode

    a++;
    return a;
}
